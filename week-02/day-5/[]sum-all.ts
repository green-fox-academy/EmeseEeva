// -  Create a variable named `ai` with the following content: `[3, 4, 5, 6, 7]`
// -  Log the sum of the elements in `ai` to the console

let ai = [3, 4, 5, 6, 7];

function add(a,b,c,d,e) {
    return a + b + c + d + e;
}

let myResult = add(3, 4, 5, 6, 7);
console.log (myResult);
